   "Докачивать Qwen3-8B (≈16,4 ГБ) ради В-5 или закрыть расширение иначе?",
   "Отвязывать load-test qwen3_5 от завершения чужой лесенки (~10 мин стенда) — кто объявляет фазу?"]}
```

Text preceding that contract is the report above.

```json
{"status": "complete",
 "assumptions": [
  "Локальный HF-кэш стенда (~/.cache/huggingface на gb10-fast) — тот самый, который монтируют контейнеры стадий (run_v12_ladder.sh:68, qwen35_gate.sh); поэтому найденные веса считаются доступными лесенке без докачки.",
  "Ось меряется на eval_ood_clean (192 задачи, judge_mean + slug_accuracy) — унаследовано из AD-1/S4-PROTOCOL, не изобретено; исход задачи бинарен (verifier multi_slug_match), поэтому p=0.5 взято как верхняя граница разброса, а не как допущение о данных.",
  "Вход SFT волны лесенки — v13_fixed (по §7 Н-1 плана); исторический раннер пока зовёт v12 — оба факта записаны в карточке раздельно.",
  "Цена eval линейна по m·n и калибрована по ADR-013 п.1 (≈2 ч на 192 задачи при n=1) — верхняя граница.",
  "Отсутствие Qwen3-8B в обоих сторах стенда — факт замера; считать её недоступной 'вообще' оснований нет, докачка не проверялась."
 ],
 "open_questions": [
  "Ось лесенки (метрика + n + порог + критерий воспроизведения): конфигурация A (m=192, n=6) или B (m=400, n=3)? Решение архитектора, ADR-052 п.2.",
  "Если B — новый eval-набор на 208 задач: это решение AD-7 с аудитом чистоты (ADR-025); кто его объявляет?",
  "Принимается ли замена критерия «2 из 3 семейств» по знаку (50 % при нуле) на «порог пройден ≥2 из 3» (0,725 %)?",
  "Н-1: нести преток SFT-кэшей на стенд в окно В-1 или чинить локальный transformers (вне стенда)?",
  "Qwen3-8B: докачивать (≈16,4 ГБ) ради волны В-5 или закрывать расширение иначе?",
  "Load-test qwen3_5 отвязать от чужой лесенки — нужна ли отдельная фаза ~10 мин стенда до В-1?"
 ],
 "conflicts_with_prior_decisions": []}
```
