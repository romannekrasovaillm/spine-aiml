# Языковой сдвиг генерации: язык корпуса против языка ответа (S3ac)

> **Вопрос узла.** Финальный CPT-чекпойнт прошёл PPL-критерий стадии (K1 `v3_general`
> ×0.9848, K2 `general_eval_k2` ×1.0671, ADR-032), но в свободной генерации отвечает
> по-английски: доля кириллицы **0.123** против **0.888** у базы (S3ab,
> `evidence/s3ab-cpt-probes.json`). Режим при этом появился (4/5 проб с `<think>`,
> 2/5 с `<tool_call>`; у базы 0/5). Нужно было числом ответить: сдвиг привнесён
> **составом микса** или это **другой механизм**, и что из этого следует делать.
>
> **Инструмент.** `tools/corpus_language_probe.py` (S3ac). Отчёт —
> `evidence/s3ac-corpus-language.json`. Метрика языка — та же, что у прибора S3ab
> (`probe_control.degenerate_metrics`): `cyrillic_share = кириллические буквы /
> (кириллические + латинские)`. Числа этого разбора сопоставимы с 0.123 и 0.888
> по определению, а не по названию.

## 1. Что измерено и на чём

| Вход | Путь | Проверка |
|---|---|---|
| домен CPT | `cpt_corpus_v10.1.txt` (211.7M символов) | sha256 `1765628e…` = хеш входа сборки микса (манифест `cpt_corpus_v12r50.txt`) |
| replay CPT | `general_replay_ru.txt` (76.1M символов) | sha256 `6497022d…` = хеш входа сборки |
| преток-кэш микса | `tok/cpt_corpus_v12r_8192_qwen25.npy` | 7332 доменных строк + 2444 replay-строки восстановлены сопоставлением с доменным кэшем по хешу строки — **не подразумеваются, а проверены** |
| преток-кэш домена | `tok/cpt_corpus_v10.1_8192_qwen25.npy` | граница «карты / траектории» — по номеру токена первого `<|im_start|>system` (id 151644 + `system\n`) в самом кэше |
| датасет SFT | `sft_train_v12.jsonl` (486 МБ, 44 949 записей) | тот же файл, что подан на стенд (SFT идёт); маска лосса — по коду `laguna_pipeline_v8.py` |
| проба S3ab | `runs/s3ab-probes-20260917/probe_control.json` | перемер ответов тем же автоматом; тексты в отчёт не попадают (AD-8) |

Разбор — двумя приборами: **(а)** потоковым конечным автоматом по маркерам формата
v12 на исходных текстах (`--mode text`), **(б)** на **строках преток-кэша**, то есть
на том, что реально училось (`--mode stream`). Две независимые оценки сходятся:
кириллица карт 0.6164 (текст) против 0.6168 (кэш), траекторий — 0.3421 в обоих.
Объём — по буквам (пул), а не по документам: у карт и траекторий разная плотность
токенов, среднее по документам соврало бы.

## 2. Числа: язык по компонентам микса

Веса — по токенам кэша (чанк 8192), ровно 80 084 992 токена:

| Компонент | Вес | Токены | Кириллица | Англ. слова | `<think>` фрагментов | Кириллица в `<think>` |
|---|---|---|---|---|---|---|
| домен: **концепт-карты** | 0.4551 | 36 443 992 | **0.6168** | 0.4385 | 0 | — |
| домен: **траектории учителя** | 0.2949 | 23 619 752 | **0.3421** | 0.7030 | 17 567 | **0.0613** |
| replay: общий язык (wiki ru) | 0.2500 | 20 021 248 | **0.9834** | 0.0250 | 0 | — |
| **поток целиком** | 1.0000 | 80 084 992 | **0.5916** | 0.4580 | 17 567 | 0.0613 |

Тезис «корпус англоязычен, поэтому модель отвечает по-английски» **числом
отклоняется**: поток на 59 % кириллический и на 98 % кириллический в replay-части.
Но английский в потоке не размазан — он **стоит в одном месте**.

## 3. Числа: где именно стоит английский

Ход `assistant` в траекториях домена разложен на части (тот же прибор):

| Часть хода assistant | Объём | Кириллица | Англ. слова |
|---|---|---|---|
| **первые 200 символов хода** (позиция генерации) | 2 176 200 симв. | **0.0003** | 0.9998 |
| открывающий ход `<think>` (10 873 из 10 881 ходов) | 135M симв. | **0.0479** | — |
| `<think>` целиком | 49 270 081 симв. | 0.0613 | 0.9504 |
| ответ вне `<think>` | 35 933 791 симв. | **0.6402** | 0.4113 |

Разрыв внутри **одного и того же хода** — 0.0003 против 0.6402. Модель обучена не
«говорить по-английски», а **начинать ответ с англоязычного рассуждения и уже в конце
переходить на русский**. Первые 200 символов хода — 336 065 английских слов против 71
русского.

**Формат трасс однотипен** (ответ на п.5 задания): 17 567 фрагментов `<think>`,
из них 17 357 (98.8 %) начинаются со слова `the`; топ-открытие
`"\nThe user asks me to explain the connection betw…"` покрывает 13.5 % фрагментов
дословно; медиана длины — 2 778 символов (550 токенов). Дословных повторов мало
(0.31 %), то есть трассы не зациклены, но **шаблон входа один** — поэтому режим и
выучивается целиком и быстро.

Языковой состав самих трасс: 0.8246 фрагментов англоязычные (кириллица < 0.05),
0.1711 смешанные, 0.0043 русские.

## 4. Механизм: что подтверждено, что осталось гипотезой

Проверки с объявленными порогами (пороги — в инструменте, не подбирались под ответ):

| Проверка | Порог | Факт | Итог |
|---|---|---|---|
| M1 «корпус не англоязычен целиком» | кириллица потока ≥ 0.5 | 0.5916 | подтверждено |
| M2 «английский стоит в генеративной позиции» | кириллица первых 200 символов хода ≤ 0.15 | 0.0003 | подтверждено |
| M3 «английский сконцентрирован в рассуждении» | траектории < карты, `<think>` < траектории | 0.3421 < 0.6168; 0.0479 < 0.3421 | подтверждено |
| M4 «окно пробы не выходит из рассуждения» | медианный `<think>` > бюджет пробы 384 токена | 550 токенов (60 % фрагментов длиннее) | подтверждено |
| M5 «SFT несёт то же рассуждение» | кириллица `<think>` SFT < 0.5 | 0.2178 | подтверждено |

Наблюдаемая генерация ложится на ту же картину: перемер проб S3ab тем же автоматом
даёт по финалу кириллицу **0.1259** (пул букв; 0.123 в S3ab — среднее по 5 пробам,
способ усреднения другой), префикс ответа 0.0251, `<think>` 0.0112; у базы 0.8738,
у instruct 0.9626. Расстояние от наблюдаемого 0.123 до генеративной позиции корпуса
(0.0003) — **0.1227**, до среднего языка потока (0.5916) — **0.4686**: сдвиг объясняется не
«английским корпусом», а тем, **какой язык обучен в старте ответа**.

**Гипотезой остаётся:**
- что именно переносится — языковой режим рассуждения как часть формата или
  распределение первых токенов после `<|im_start|>assistant`: разложение по позициям
  **внутри** рассуждения не измерялось, обе версии дают одно и то же наблюдаемое;
- причинная доля домена против replay: руки S3m меняли **долю**, но не **язык** трасс,
  поэтому «вес домена × язык трасс» разведены не полностью;
- почему PPL русского текста не падает при англоязычном рассуждении: PPL считает
  продолжение **данного** текста, проба — свободную генерацию; расхождение линий —
  факт, но не приговор ни одной из них.

**Чего числа не показывают.** Что произошло бы при русскоязычных трассах: рука с тем
же весом домена, но переведёнными трассами, не ставилась. Это следующий замер, а не
вывод этого.

## 5. Вердикт и что делать

**Лечение: и состав микса, и пост-тренировка — и правка датасета SFT.** Варианты и их
цена:

**(а) Состав микса.** Англоязычное рассуждение приходит из траекторий учителя:
23.6M токенов (29.5 % всего потока) и 39.2 % доменной части. Варианты внутри (а):

| Вариант | Что даёт | Цена |
|---|---|---|
| снизить долю домена (как `v12r50`, S3m) | replay 50 %: режим пропадает, язык цел (руки S3m/S3ab: «режима нет / язык цел» + вырождение) | теряется то, что S3ab считает приобретением (режим, `uniq4` 0.87 против 0.28 у базы); домен выучен хуже |
| перевести/перегенерировать трассы в русский | снимает носитель, не трогая долю | нужен учитель на 23.6M токенов (прогон уровня CPT) либо фильтр: отбросить англоязычные `<think>` — домен теряет 39 % токенов, вместе с ними уходит и режим |
| добавить русскоязычные трассы в replay | режим сохраняется, носитель меняет язык | русского reasoning-корпуса под формат v12 нет; генерация учителем ≈ 20M токенов |

**(б) Пост-тренировка.** SFT как он подан **не снимет сдвиг, а закрепит его**: датасет
`sft_train_v12.jsonl` несёт тот же англоязычный `<think>` — кириллица его think-части
0.2178 (в CPT-траекториях 0.0479), англ. слова 0.8417, первые 200 символов хода
0.1739. При этом `<think>` **входит в лосс**: метки начинаются с хода assistant
(`labels[:first_asst + 2] = -100`, `laguna_pipeline_v8.py:300`), маскируются только
system/user и `<tool_response>`, то есть 175.2M символов англоязычного рассуждения —
целевые токены SFT. Ожидание «SFT вылечит язык» числами не поддержано: без правки
датасета SFT сдвинет генерацию в лучшем случае к 0.17…0.22 кириллицы.

**Рекомендация следующего шага** (цена в порядке возрастания):

1. **Не ждать SFT как лечения языка** — это уже вывод, а не гипотеза (M5). Считать
   регрессию по языку генерации незакрытой до правки данных.
2. **Дешёвая проверка причинности** (≈1.5–2 ч стенда: 2000 шагов при 2.57 с/шаг по
   замеру S2 плюс проба): CPT-рука 25 % replay, где `<think>` траекторий переведены на
   русский (или отфильтрованы), и та же проба S3ab. Это отделяет «язык трасс» от
   «доли домена» — то, что сейчас гипотеза.
3. **Микс** — по ADR-003, отдельным решением: цена перевода/генерации трасс ≈ стоимость
   CPT-прогона (23.6M токенов учителя), цена фильтра — потеря режима.
4. **SFT-датасет** правится тем же носителем, что и CPT (иначе пункт 3 обесценится).
5. **Критерий стадии** (ADR-035): генеративная компонента уже введена — этот узел
   подтверждает, что она читает ровно то, что PPL не видит, и даёт для неё опору.

## 6. Границы этого разбора

- Решение о миксе здесь **не принимается** (ADR-003): измерено, что носитель есть и
  где он; какой из вариантов (а) выбрать — отдельный вопрос с отдельной ценой.
- Критерии стадии не менялись (ADR-035).
- Поля `<tool_call>`/`<tool_response>` в отчёте помечены как ненадёжные: их маркеры
  цитируются внутри карт и внутри `<tool_response>`, из-за чего области склеиваются
  (диагностика в отчёте: 12 500 открытий `<tool_call>` внутри уже открытого). На
  метрики `<think>` и на вердикт это не влияет — генерирующий фрагмент считается
  отдельным счётчиком (10 873 хода из 10 881), не зависящим от парности маркеров.
- Текстовый разбор replay читает **весь** файл `general_replay_ru.txt` (76.1M символов), а
  разбор потока — только обученный префикс (2444 чанка = 20.02M токенов из 3493 чанков
  источника). Числа расходятся не из-за прибора: кириллица всего файла 0.9811 против
  0.9834 у префикса — вики-текст однороден, и вывод от выбора границы не зависит.
- Домен-часть корпуса = 39.2 % токенов траекторий учителя — это факт сборки
  (`build_cpt_v101.log`), разбор его подтверждает независимо: граница карт/траекторий
  в кэше даёт 36.44M / 23.62M токенов, а лог называет 36.74M / 23.65M — расхождение
  0.8 % и 0.1 % объясняется тем, что лог считал текст до разделителей `---` и до
  хвостовой отсечки чанкера.
- Корпуса и датасет SFT читались по месту, только чтение (AD-4); копий не создавалось;
  GPU не занималась — разбор целиком на CPU. Стенд GB10 как вычислитель не задействован
  (там SFT): `gb10-shared` — NFS-монтирование его диска, и чтение шло по сети, как это
  делают приборы S3h/S3m; ни SSH, ни GPU стенда не трогались.
