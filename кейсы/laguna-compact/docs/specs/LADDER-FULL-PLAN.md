# LADDER-FULL-PLAN.md — исполнимый план полной лесенки (возврат из `Deferred`)

- Date: 2026-09-21
- Status: Proposed (до решения владельца по §§6–7) — **§6 закрыт решением `ADR-055`**, §5 — решением `ADR-056` (обе оси/область объявлены до первого прогона В-1)
- Обновление 22.09.2026 (дельта S3bj): **область к 1.11.2026 объявлена** `ADR-056` — волны В-0/В-1/В-4 входят, В-2/В-3/В-5 отложены за дату (§5, §5.1а, §5.2); **цена eval исправлена** (36 ч → 72 ч, §5.1); **семантика сида «форк»** — из открытого вопроса в решение (§4.2); ось `ADR-055` к волне В-0 не применяется (§5.3)
- Основание решения: **владелец, 21.09.2026 — «нужна полная лесенка»**
- Решение: `docs/adr/ADR-052-vozvrat-polnoi-lesenki-iz-deferred-obyom-sostav-i-usloviya.md`
- Спайн: `ARCHITECTURE-SPINE.md` (AD-1, AD-5, AD-9; раздел `Deferred`)
- Родительский контур (read-only): `~/gb10-shared` — `EXPERIMENT_PLAN.md` §2,
  раннеры `run_v12_ladder.sh` / `run_v4_ladder*.sh` / `run_seeded_ladder.sh`
- Что здесь **не** делается: стенд не занимается, обучение не запускается, чужие ветви
  и остановленные артефакты не трогаются. Это план, а не прогон.

## 0. Что меняет решение владельца и чего оно не меняет

**Меняет.** Условие возврата лесенки из `Deferred` («результат ревизии принят и требуется
статистическая мощность») **снимается решением владельца, а не исполнением** — точка
решения S4 на 21.09.2026 не пройдена (SFT-стадия идёт). Это надо назвать прямо: лесенка
разворачивается **до** того, как пилот ревизии дал результат, и цена этого — ревизия и
лесенка начинают конкурировать за один и тот же стенд (AD-5: одна нагрузка за раз).

**Не меняет.** Узкий протокол ревизии (AD-1) остаётся в силе: он по-прежнему запрещает
заявления о превосходстве между семействами и размерами **для ревизии**. Лесенка —
другой предмет с другой осью, и её ось обязана быть объявлена **до первого прогона**
(ADR-052 п.2; см. §6 ниже). Запрет AD-1 не отменяется — он ограничивается областью.

## 1. Состав лесенки — по фактам, а не по памяти

### 1.1 Точный перечень существует, и это 12 моделей

Точный список **зафиксирован** — он не «подразумевался». Два независимых носителя:

1. `/home/user/gb10-shared/EXPERIMENT_PLAN.md:16-19` (дата 2026-08-02, «зафиксирован ДО запуска»):

```
- Модели × сиды (42 1337 2024 777 31415):
  - Qwen2.5: 0.5B, 1.5B, 3B, 7B(LoRA r16) — batch 8/4/2/1
  - Qwen3: 0.6B, 1.7B, 4B, 8B(LoRA) — batch 8/4/2/1
  - Qwen3.5: 0.8B, 2B, 4B, 9B(LoRA) — batch 8/4/2/1 (после load-test архитектуры)
```

2. `/home/user/gb10-shared/run_v12_ladder.sh:3` — исполняемый носитель (не план, а код):

```
# 12 моделей: Qwen2.5 (0.5/1.5/3/7B) + Qwen3 (0.6/1.7/4/8B) + Qwen3.5-Base (0.8/2/4/9B).
```

Расхождение между двумя носителями есть, и оно существенное: `EXPERIMENT_PLAN.md` §2
объявляет **LoRA r16** для 7B/8B/9B, а исполняемый `run_v12_ladder.sh` LoRA не использует
вовсе (полный fine-tune, батч сведён к 1, `LAGUNA_MEM_FRACTION=0.7` на верхнем размере).
Разрешение — §1.4.

### 1.2 Таблица кандидатов: семейство × размер × веса × токенизатор

Проверка весов — по общему хранилищу (`/home/user/gb10-shared/models-store`, он же
`~/.cache/huggingface` через симлинк; см. `docs/specs/DISK-PLAN.md:25`).

| # | short | HF id | Семейство | Размер | Веса в общем сторе | Токенизатор | Батч (v12-раннер) |
|---|---|---|---|---|---|---|---|
| 1 | `qwen25-05b` | `Qwen/Qwen2.5-0.5B` | Qwen2.5 | 0.5B | **есть** (legacy-layout `models--Qwen--Qwen2.5-0.5B`, 990 МБ, 290 тензоров) | **есть** | 2 |
| 2 | `qwen25-15b` | `Qwen/Qwen2.5-1.5B` | Qwen2.5 | 1.5B | **есть** (`hub/…/Qwen2.5-1.5B`, 3,09 ГБ, 338 тензоров; плоский layout без `snapshots/`) | **есть** | 1 |
| 3 | `qwen25-3b` | `Qwen/Qwen2.5-3B` | Qwen2.5 | 3B | **не найдено** (каталога нет ни в legacy, ни в `hub/`) | **есть** (кэш v12r `qwen25`) | 1 |
| 4 | `qwen25-7b` | `Qwen/Qwen2.5-7B` | Qwen2.5 | 7B | **нет** (в сторе `Qwen2.5-7B-**Instruct**` и только метаданные: шардов 4 × `*.incomplete` по 0 Б) | **есть** | 1 |
| 5 | `qwen3-06b` | `Qwen/Qwen3-0.6B` | Qwen3 | 0.6B | **не найдено** | **есть** (кэш v12r `qwen3`, vocab 151 671) | 2 |
| 6 | `qwen3-17b` | `Qwen/Qwen3-1.7B` | Qwen3 | 1.7B | **не найдено** | **есть** | 2 |
| 7 | `qwen3-4b` | `Qwen/Qwen3-4B` | Qwen3 | 4B | **только метаданные**: `config.json` + `tokenizer.json` + index (заявлено 8,04 ГБ), шарды — 2 × `*.incomplete` | **есть** | 1 |
| 8 | `qwen3-8b` | `Qwen/Qwen3-8B` | Qwen3 | 8B | **нет** (снапшот пустой, блоб 0 Б) | **есть** | 1 |
| 9 | `qwen35-08b` | `Qwen/Qwen3.5-0.8B-Base` | Qwen3.5 | 0.8B | **не найдено** | **есть** (кэш v12r `qwen35`, vocab 248 079) | 2 |
| 10 | `qwen35-2b` | `Qwen/Qwen3.5-2B-Base` | Qwen3.5 | 2B | **не найдено** | **есть** | 1 |
| 11 | `qwen35-4b` | `Qwen/Qwen3.5-4B-Base` | Qwen3.5 | 4B | **не найдено** (есть только `ariadna-{cpt,sft,grpo}-v1` с `config.json` без весов) | **есть** | 1 |
| 12 | `qwen35-9b` | `Qwen/Qwen3.5-9B-Base` | Qwen3.5 | 9B | **нет HF-весов**; есть только GGUF `Qwen3.5-9B-Q8_0.gguf` (9,53 ГБ, не годится для CPT/SFT) | **есть** | 1 |

**Прямой ответ по наличию.** В **общем** хранилище подтверждены веса **двух** моделей из
двенадцати (0.5B, 1.5B). Токенизаторы подтверждены для **всех трёх семейств** — и это
факт замера, а не надежда: кэши `datasets/tok/cpt_corpus_v12r_8192_{qwen25,qwen3,qwen35}.npy`
и `sft_train_v12_8192_{qwen25,qwen3,qwen35}.npz` существуют, а претокенизация шла
**офлайн** (`HF_HUB_OFFLINE=1`, `run_v12_ladder.sh:186`) — значит токенизаторы трёх
семейств в момент сборки кэшей были на месте.

**Граница этого факта (важно).** Общий стор — стор **рабочей станции 4080**
(`models-store/README.md:3`), а не стенда. Веса лесенки жили на стенде `gb10-fast`
(свой `/home/user`), и их наличие оттуда **не проверено** этой задачей. Проверка —
одна read-only команда на стенде (минуты, GPU не занимает): §10, пункт «В-1».
Отсутствие в общем сторе — не доказательство отсутствия на стенде, но и не оправдание
планировать по памяти.

### 1.2а Замер 22.09.2026 (Н-3, дельта S3bg): на стенде есть **11 из 12**

Проверка выполнена, и она **меняет вывод §1.2/§1.3**: неизвестность была не в
«сколько весов существует», а в том, **в каком сторе их искать**. Прибор
`tools/inventory_stand_weights.py` (read-only: `scandir`/`stat`, чтение `config.json`,
заголовков `safetensors`, `tokenizer.json`; стенд не занимался, GPU не трогался)
обошёл оба стора. Ключ: `~/.cache/huggingface` **на рабочей станции — симлинк** на
общий стор (NFS `10.55.0.2`), а **на стенде это отдельный локальный каталог**
(`/dev/nvme0n1p2` — тот же диск, что экспортируется как `gb10-shared`, но другая ветка
дерева). Именно в нём и лежат веса лесенки: каталог `Qwen2.5-7B` в общем сторе
отсутствует, а в локальном кэше стенда — 4 шарда, 15,23 ГБ.

| # | Позиция | §1.2 (общий стор) | **замер на стенде 22.09** | Где |
|---|---|---|---|---|
| 1 | `qwen25-05b` | есть | **есть** (0,99 ГБ, 1 шард) | локальный кэш стенда |
| 2 | `qwen25-15b` | есть | **есть** (3,09 ГБ, 1 шард) | локальный кэш стенда |
| 3 | `qwen25-3b` | не найдено | **есть** (6,17 ГБ, 2 шарда) | локальный кэш стенда |
| 4 | `qwen25-7b` | нет (только Instruct-метаданные) | **есть** (15,23 ГБ, 4 шарда, **база, не Instruct**) | локальный кэш стенда |
| 5 | `qwen3-06b` | не найдено | **есть** (1,50 ГБ, 1 шард) | локальный кэш стенда |
| 6 | `qwen3-17b` | не найдено | **есть** (4,06 ГБ, 2 шарда) | локальный кэш стенда |
| 7 | `qwen3-4b` | только метаданные | **есть** (8,04 ГБ, 3 шарда) | локальный кэш стенда |
| 8 | `qwen3-8b` | нет (пустой снапшот) | **нет** (пустой снапшот в общем сторе, каталога нет и в локальном кэше) | — |
| 9 | `qwen35-08b` | не найдено | **есть** (1,75 ГБ, 1 шард) | локальный кэш стенда |
| 10 | `qwen35-2b` | не найдено | **есть** (4,55 ГБ, 1 шард) | локальный кэш стенда |
| 11 | `qwen35-4b` | не найдено | **есть** (9,32 ГБ, 2 шарда) | локальный кэш стенда |
| 12 | `qwen35-9b` | нет HF-весов (только GGUF) | **есть** (19,31 ГБ, 4 шарда) | локальный кэш стенда |

**Что проверено механизмом, а не глазами** (полный отчёт —
`evidence/ladder-weights-inventory.json` + `.md`, там же sha256 весов и токенизаторов):

- **файлы — настоящие веса, а не указатели LFS**: у каждого шарда разобран заголовок
  `safetensors` (число тензоров, сумма данных); у 7 многошардовых позиций сумма
  сошлась с `metadata.total_size` индекса **точно**;
- **`*.incomplete` и нулевых блобов нет ни в одной позиции** — класс дефекта, из-за
  которого §1.2 видел «только метаданные», здесь отсутствует;
- **размеры дают параметры, совпадающие с классами**: 0,494 / 1,544 / 3,086 / 7,616 /
  0,752 / 2,032 / 4,022 / 0,873 / 2,274 / 4,660 / 9,653 B — то есть это те модели,
  что заявлены в §1.1, а не соседние;
- **токенизатор на семейство один**: `tokenizer.json` побайтово один и тот же внутри
  Qwen2.5 (sha256 `c0382117…`), Qwen3 (`aeb13307…`), Qwen3.5 (`fe000e3e…`) — значит
  трёх претокен-кэшей (по тегу на семейство) достаточно для всех размеров семейства.

**Следствие для §1.3 и §7 (Н-4).** Докачка нужна **одной** модели из двенадцати
(`Qwen3-8B`), а не десяти: исполнимый состав §1.4 (0.5/0.6/0.8/1.5/3/7B) лежит на
стенде целиком. Объём докачки — числом: ≈ **16,4 ГБ** (8,19 B параметров × 2 байта,
§2.3) при 825 ГБ свободных на `gb10-shared` (`docs/specs/DISK-PLAN.md`), то есть
докачка перестаёт быть внешней зависимостью критического пути для волн В-0…В-4 и
остаётся таковой только для расширения В-5.

**Решение по `Qwen3-8B` (ADR-055 шаг 5): не докачивать.** Что именно отсутствует —
названо и не округляется: **HF-весов нет** (в общем сторе пустой снапшот — блоб 0 Б,
в локальном кэше стенда каталога нет вовсе), вес докачки — **≈16,4 ГБ**. Это
**не является предусловием** волн В-0…В-4 и не держит критический путь: вся восьмёрка
позиций исполнимого состава (§1.4) на стенде есть. Расширение (**В-5**) открывается
**только после В-3** и только при **различимом по оси §6.1** эффекте — если эффект
окажется на границе (2–5 п.п.), сначала идёт решение о расширении **набора** (ADR-025),
а не докупка весов. Отсутствие веса фиксируется и в `WORK-BACKLOG.md`.

**Граница замера (названа, чтобы её не приписали замеру).** Прибор доказывает
**наличие и целостность файлов**, а не **загружаемость архитектуры в стек**: для
`qwen3_5` это отдельный вопрос — см. §1.2б.

### 1.2б Риск `qwen3_5`: что известно сейчас и чего нет

`config.json` всех четырёх Qwen3.5-Base подтверждает не «Qwen3», а **свою**
архитектуру: `model_type: qwen3_5`, `architectures: ["Qwen3_5ForConditionalGeneration"]`,
`transformers_version: 4.57.0.dev0`, гибрид `linear_attention` ×3 + `full_attention`,
MTP-слой (`mtp_num_hidden_layers: 1`), наличие `vision_config` и `image_token_id`
(мультимодальная надстройка в той же конфигурации). Это **не** плоский декодер, и
риск из §9 остаётся содержательным.

Факты, которые уже есть (read-only, со стенда):

- **load-test `qwen35_gate.sh` не выполнялся ни разу.** Гейт ждёт строку
  `V4 LADDER QWEN3 COMPLETE`, а лесенка Qwen3 её не выдала: `qwen35_gate.log` (1911 Б,
  07.08.2026) содержит 14 циклов ожидания «Веса скачаны. Жду финиша лесенки Qwen3» и
  ни одной строки запуска load-test; файлов `qwen35_loadtest.log` и
  `qwen35_unsupported.txt` нет вовсе. То есть **вердикта** у гейта нет — ни
  положительного, ни отрицательного.
- **Косвенное свидетельство поддержки есть**: 28.08.2026 стадия CPT на
  `Qwen/Qwen3.5-2B-Base` дошла до `DONE` (80 шагов, ≈29 мин; `v12_ladder_status.log`),
  то есть **HF-путь (`AutoModelForCausalLM` + `trust_remote_code`) архитектуру взял**.
  Свидетельство косвенное: чекпойнт и лог той стадии с тех пор удалены (в каталоге
  прогона остался только `run_manifest.json`), а полный прогон 04–05.09 на тех же
  весах упал (`rc=1` у 0.8B, пустой `cpt.log`) — причину те логи не несут.
- **vLLM-путь не подтверждён ничем**: RL-стадия идёт через vLLM, а именно его load-test
  в гейте и не выполнялся.

Поэтому проверка остаётся **непроверенной фазой** волны В-1 (как и требует §7): она
требует стенда и потому в S3bg не выполнялась (AD-9 — стенд занят SFT). Чтобы она
стала дешёвой, её надо отвязать от завершения чужой лесенки: нужен вердикт
«HF_LOAD_OK + VLLM_LOAD_OK» на 0.8B, ~10 минут стенда перед волной В-1.

#### 1.2б-1 Фаза подготовки В-1: load-test `qwen3_5` (`ADR-055` шаг 4; дельта S3bh)

**Отвязка выполнена — носитель `tools/qwen35_gate.sh`** (в кейсе; прежний
`~/gb10-shared/qwen35_gate.sh` остаётся историческим носителем и не правится —
чужой контур read-only). Что именно изменилось против прежнего гейта:

| Было (`~/gb10-shared/qwen35_gate.sh`) | Стало (`tools/qwen35_gate.sh`) |
|---|---|
| Ждал **чужое событие**: строку `V4 LADDER QWEN3 COMPLETE` в журнале лесенки Qwen3, плюс маркер `ALL_QWEN35_BASE_DOWNLOADED` | **Не ждёт ничего**: ни флага паузы, ни журнала лесенки, ни маркера докачки. Проба — самостоятельный шаг подготовки, выполняется по окну |
| При успехе **сам поднимал** `run_v4_ladder_qwen35.sh` — то есть занимал стенд нагрузкой | Даёт **вердикт**, а не нагрузку. Автозапуска лесенки нет |
| Ожидание без предела: 14 циклов `sleep 300` к 07.08.2026, проба не выполнялась ни разу | Ни одного `sleep` в исполняемом коде; проба ограничена `timeout` (900 с) и `docker run --rm` |
| Вердикт выражался отсутствием файлов (`qwen35_loadtest.log` / `qwen35_unsupported.txt`) | Вердикт — один носитель `<state-dir>/qwen35-loadtest.json` (+ сырой журнал и его sha256 в вердикте) |
| Два состояния читались как «файла нет → наверное, поддержано/не поддержано» | **Три состояния словом**: `not-run` (пробы не было — вердикта **нет**), `supported`, `unsupported`; коды возврата 2 / 0 / 1 |
| Занятость не проверялась вовсе | Единственное, что может отказать `run`: **факт** от стража сериализации (`tools/check_gb10_serialization.sh`, AD-5) — одна тренировочная нагрузка; неизмеренная занятость — **отказ закрыто**. Платформенный `llama-server` нагрузкой не считается: проба идёт с `gpu_memory_utilization=0.30` именно затем, чтобы ужиться с ним |

**Предмет пробы не изменён**: тот же образ `nvcr.io/nvidia/pytorch:26.07-py3-vllm`, те же
две проверки (`HF_LOAD_OK` + `VLLM_LOAD_OK`), тот же `max_model_len=4096` и
`gpu_memory_utilization=0.30`, что и в прежнем гейте, — меняется только то, **что её
запускает**. **Носитель вердикта один** (ADR-023 п.10) — `evidence/qwen35-loadtest.json`
вместе с сырым журналом и его sha256; «файла нет» больше не читается как вердикт.

**Условие запуска (названное).** Проба выполняется **перед волной В-1**, в окне стенда
**≈10 мин**, при **отсутствии живой тренировочной нагрузки** (AD-5/AD-9). Проверяется
фактом сенсором гейта, а не флагом; **стенд занимается только на эти ≈10 мин**. Команда:

```bash
bash tools/qwen35_gate.sh status                      # вердикт по факту, стенд не трогается
bash tools/qwen35_gate.sh run --host gb10-fast        # сама проба (≈10 мин окна)
```

**В S3bh проба не запускалась** — стенд занят полной SFT-стадией (`sft-v13-2e6-20260921-2054`),
и запуск был прямо запрещён заданием. Поэтому на 22.09.2026 состояние — **`not-run`**:
вердикта нет, и это названо «не проверено», а не «не поддержано».

### 1.3 Что из этого следует для состава

Лесенка **не может быть запущена в составе 12 моделей немедленно**: для 10 из 12 нет
подтверждённых весов. Докачка — не проблема сама по себе (Qwen2.5-3B, Qwen3-0.6B/1.7B/4B/8B,
Qwen3.5-Base — публичные модели), но это **внешняя зависимость в критическом пути**:
у Qwen3.5 стоит отдельный риск неподдержки архитектуры `qwen3_5` образом
(`EXPERIMENT_PLAN.md:61`, гейт `~/gb10-shared/qwen35_gate.sh`).

**Поправка 22.09.2026 (не переписывая написанного).** Обе части этого абзаца замер
§1.2а скорректировал, и по-разному:
- «для 10 из 12 нет весов» — **неверно для стенда**: там есть 11 из 12, включая весь
  исполнимый состав §1.4. Докачка в критическом пути волн В-0…В-4 **не стоит**;
  осталась одна позиция (`Qwen3-8B`, ≈16,4 ГБ) и только для расширения В-5 — и она
  **решением не докачивается** (ADR-055 шаг 5; §1.2а, следствие);
- «внешняя зависимость» сместилась: место докачки занял **load-test архитектуры** —
  он и есть настоящий (и до сих пор не выполненный) гейт волны В-1, §1.2б.

### 1.4 Заявленный состав против исполнимого — два разных числа

| | Моделей | Что это | Обоснование |
|---|---|---|---|
| **Заявленный** | **12** | `EXPERIMENT_PLAN.md` §2 = `run_v12_ladder.sh:3` | Точный перечень существует, выдумывать не нужно (§1.1) |
| **Исполнимый** | **6** | 0.5B, 0.6B, 0.8B (класс ≤1B, три семейства) + 1.5B, 3B (размер внутри Qwen2.5) + 7B (верхний размер) | Полные 12 в текущем рецепте стоят **годы** (§2.4); 6 покрывают обе заявленные оси (семейство и размер) и обе гипотезы §1 `EXPERIMENT_PLAN` |

Почему именно **6**, а не «12, но по волнам»: обе оси (`EXPERIMENT_PLAN.md:8` — «эффект
воспроизводится между семействами и устойчив к смене сида»; `:28` — «сравнение по размеру
внутри семейства; по семейству при сопоставимых классах (4B-класс: Qwen2.5-3B / Qwen3-4B /
Qwen3.5-4B)») проверяются минимально на 3 семействах × 1 классе + 3 размерах внутри одного
семейства. Остальные 6 моделей — **расширение**, а не условие: они не добавляют новых
осей, только плотность точек. Это делает лесенку останавливаемой после любой волны (§5).

## 2. Цена: `CPT + SFT + k × RL` на модель

### 2.1 Исходный замер (0.5B, текущий рецепт ревизии)

| Стадия | Измерено | Откуда |
|---|---|---|
| CPT | 24 156 с = **6,71 ч** (9 776 шагов) | CPT-стадия ревизии (ADR-032) |
| SFT | 66 157 шагов × 3,94 с = **72,41 ч** | Набор `v13_fixed`, 44 105 примеров, батч 2, 3 эпохи |
| RL | 500 шагов × 126 с = **17,50 ч** | Среда на GB10: ×1,02–1,06 к базе 118–124 с/шаг |
| eval | **≈2 ч** | ADR-013 п.1 (192 задачи + `eval_sft` на том же чекпойнте) |
| **Итого на сид** | **98,62 ч = 4,11 сут** | |

Проверка SFT прямым замером (не пересказом): `runs/sft-v13-2e6-20260921-1748/logs/loss_trace.jsonl`,
шаги 1→799 = 3 084 с / 798 шагов = **3,865 с/шаг** (замер снят на живом прогоне 21.09.2026,
шаг 799 достигнут). Число в таблице (3,94) включает пробы каждые 50 шагов и не противоречит
прямому замеру.

### 2.2 Допущение экстраполяции (названо явно)

**Единственное допущение:** время стадии пропорционально числу параметров —
`T(N) = T(0,5B) × N/0,5B`, при неизменных данных, длине контекста и числе шагов.
Основание: compute ∝ параметры × токены; выбор батча влияет на **память**, а не на
суммарные токены (батч 2 при 0,5B и батч 1 при 1,5B дают одно и то же число токенов
на эпоху), поэтому сведение батча к 1 у больших размеров времени почти не меняет.

**Чего допущение не учитывает (и потому оценка — нижняя граница):** термотроттлинг на
длинной дистанции; рост длины генерации у больших моделей на RL-стадии (у 0,5B генерация
уже упирается в потолок хода — 97,6 %, ADR-011 п.2); накладные расходы ресника vLLM
растут с размером весов.

### 2.3 Таблица по размерам (один сид; рецепт §2.1)

| Модель | N/0,5B | CPT, ч | SFT, ч | RL, ч | eval, ч | **сид, сут** | k=3 наивно, сут | k=3 форк, сут |
|---|---|---|---|---|---|---|---|---|
| Qwen2.5-0.5B | 1 | 6,7 | 72,4 | 17,5 | 2,0 | **4,11** | 12,3 | 5,6 |
| Qwen3-0.6B | 1,2 | 8,1 | 86,9 | 21,0 | 2,4 | **4,93** | 14,8 | 6,7 |
| Qwen3.5-0.8B | 1,6 | 10,7 | 115,9 | 28,0 | 3,2 | **6,57** | 19,7 | 8,9 |
| Qwen2.5-1.5B | 3 | 20,1 | 217,2 | 52,5 | 6,0 | **12,33** | 37,0 | 16,7 |
| Qwen3.5-2B | 4 | 26,8 | 289,6 | 70,0 | 8,0 | **16,44** | 49,3 | 22,3 |
| Qwen2.5-3B | 6 | 40,3 | 434,5 | 105,0 | 12,0 | **24,66** | 74,0 | 33,4 |
| Qwen3-4B / Qwen3.5-4B | 8 | 53,7 | 579,3 | 140,0 | 16,0 | **32,87** | 98,6 | 44,5 |
| Qwen2.5-7B | 14 | 93,9 | 1 013,7 | 245,0 | 28,0 | **57,53** | 172,6 | 78,0 |
| Qwen3-8B | 16 | 107,4 | 1 158,6 | 280,0 | 32,0 | **65,75** | 197,2 | 89,1 |
| Qwen3.5-9B | 18 | 120,8 | 1 303,4 | 315,0 | 36,0 | **73,96** | 221,9 | 100,2 |

«**наивно**» = k полных прогонов CPT→SFT→RL (так устроен `run_seeded_ladder.sh`:
цикл по сидам снаружи, внутри — все стадии). «**форк**» = CPT+SFT один раз на модель,
сид множит **только** RL (`CPT+SFT+eval + k×RL`) — так устроен RL-путь контура: RL стартует
с готового SFT-чекпойнта (`input_checkpoint` в `full_sft_params.json`), и это ровно тот
механизм, которым снята RL-разведка (`sft_checkpoint_final.pt` → 500 шагов, ADR-010).
Форк — **не мелочь**: он превращает дополнительные сиды из 4,11 сут в 0,73 сут на 0,5B.

### 2.4 Цена заявленного состава (сумма факторов 12 моделей = 84,2)

| Вариант | Календарь | В годах |
|---|---|---|
| 12 × 5, наивно | **1 730 сут** | **4,74 года** |
| 12 × 5, форк | 592 сут | 1,62 года |
| 12 × 3, наивно | 1 038 сут | 2,84 года |
| 12 × 3, форк | 469 сут | 1,28 года |
| 12 × 3, форк + SFT 1 эпоха вместо 3 | 299 сут | 0,82 года |

**Это и есть настоящий смысл слова «Deferred».** В спайне (`ARCHITECTURE-SPINE.md:112`)
написано «стоимость календаря» — теперь у этой фразы есть число: заявленный состав в
текущем рецепте — **годы стенда**, а не недели. Любой исполнимый план обязан быть
сокращением, и это сокращение надо объявить решением, а не получить по факту нехватки времени.

## 3. Память по стадиям и предел стенда

### 3.1 Измерено

| Величина | Значение | Откуда |
|---|---|---|
| unified-память стенда | **121,7 ГБ** | `check_resource_owner.sh` в `chain.log` стадии |
| доступно на старте SFT (21.09) | **71,7 ГБ** | там же (порог SFT — 50 ГБ) |
| постоянно занято платформой (llama-server) | **≈27,5 ГБ** | комментарий `run_v12_ladder.sh:210` |
| пороги стадий | CPT **≥35**, SFT **≥50**, RL **≥49** ГБ свободных | ADR-008 п.2, ADR-011 п.2 |
| SFT на 0,5B | **≈38 ГБ** стадии (замер площадки 21.09) | текущая стадия |
| SFT на 0,5B (смоук S2, батч 2 × 16 384) | пик 83,5 ГБ занято, **+44,4 ГБ** за стадию | ADR-008, `evidence/s2-smoke.json` |
| CPT на 0,5B | **+30,2 ГБ** за стадию | там же |
| RL на 0,5B | **+42,78 ГБ**, свободных падало до 39,92 ГБ | ADR-011, `evidence/s3-rl-probe.json` |
| **RL на 7B** | **PyTorch 81 ГБ + vLLM 31 ГБ = 112 / 121,7 ГБ → CUDA OOM** | `run_v12_ladder.sh:22` (инцидент лесенки) |

### 3.2 Ответ: до какого размера помещается

| Размер | Полный fine-tune, батч 1 × 8192 | Основание |
|---|---|---|
| 0,5B | **влезает** | измерено (батч 2, порог 50 соблюдён) |
| 1,5B | **влезает** | исторически исполнено лесенкой v12 (батч 1); в ревизии **не измерено** |
| 3B | **влезает, запас не измерен** | исторически исполнено (батч 1); при платформенном сервере 27,5 ГБ свободных остаётся ≈67 ГБ — хватит или нет, **не проверено** |
| 7B | **не влезает** | OOM на RL: 112 из 121,7 ГБ |
| 8B / 9B | **не влезает** | хуже 7B по параметрам; RL — самая тяжёлая стадия, потому что держит torch **и** vLLM одновременно |

**Короткий ответ:** **до 3B включительно** при полном fine-tune (батч 1, `max_len` 8192)
— при том, что 3B исполнялся исторически, но запаса в ревизии никто не мерил. **7B и выше
не помещаются** без смены режима.

**Что нужно для больших (в порядке предпочтения):**
1. **LoRA r16** — так и было заявлено в `EXPERIMENT_PLAN.md:17-19` для 7B/8B/9B и **не
   реализовано** в исполняемом раннере. Снимает state оптимизатора (главный вклад), не
   снимает активации.
2. **Снижение KV-кеша vLLM** — контур уже это делал: 8 ГБ вместо 16 ГБ после OOM на 7B.
3. **Снижение `max_len`/батча** — прямая цена по сопоставимости (другой бюджет = другая
   ось; у ревизии это отдельное решение, ADR-045).
4. **Оффлоад/шардирование** — на одной GPU (DGX Spark) это CPU-offload оптимизатора;
   цена — скорость, и она в текущем рецепте не измерена.
5. **Аренда внешних GPU** — альтернатива в ADR-052; выносит лесенку из периметра, где
   данные `internal_only` (AD-8) — то есть требует отдельного решения по границе.

## 4. Сиды: одно число и цена разницы

### 4.1 Противоречие в документах (три разных числа)

| Носитель | Число | К чему относится |
|---|---|---|
| `ARCHITECTURE-SPINE.md:112` (Deferred) | «12 моделей × **5 сидов**» | полная лесенка |
| `~/gb10-shared/EXPERIMENT_PLAN.md:16` | «сиды (42 1337 2024 777 31415)» | сетап лесенки v4/v5 |
| `run_v4_ladder*.sh:11`, `run_seeded_ladder.sh:11` | `SEEDS="42 1337 2024 777 31415"` | **исполняемый** носитель: 5 сидов |
| `docs/specs/REVISION.delta.md:41-42` | «полный цикл по **3 сида** (решение R3, 14.09.2026)» | ревизия ≤1B |
| `ADR-002`-круг, `docs/HYPOTHESES-WATCH.md:38` | «**3–4 прогона**, `mean pass@1` ± stderr» | пересмотр eval-протокола, фаза 2 |
| `run_v12_ladder.sh:13` | `SEED="${SEED:-42}"` — **один сид на вызов** | исполняемый полный рецепт |

Ключевой факт, который снимает противоречие: **пять сидов — свойство дешёвого рецепта,
а не решение о статистике.** Пятисидовые раннеры (`run_v4_ladder*.sh`, `run_seeded_ladder.sh`)
идут с `CPT_STEPS=80 SFT_STEPS=300 MAX_LEN=4096`, а исполняемый полный рецепт
(`run_v12_ladder.sh`, где CPT — полный проход 9 776 чанков) по умолчанию берёт **один сид**.
То есть «×5» было позволительно ровно потому, что прогон стоил часы; в текущем рецепте
тот же множитель стоит годы (§2.4).

### 4.2 Решение: **k = 3**, семантика сида — **форк**

Одно число для лесенки — **три сида**. Обоснование:

1. **Три — минимум, при котором ось AD-1 вообще определена.** AD-1 требует «≥1 std по сидам»
   и «**k-из-k** отдельно от per-run». При k=2 std вырождается, k-из-k неотличим от min;
   k=3 даёт обе величины и разброс. Это же число уже принято решением владельца R3
   (14.09.2026) — лесенка **наследует** число, а не изобретает новое.
2. **Пять — не независимое решение.** Оно пришло из `EXPERIMENT_PLAN.md` §2 (02.08.2026),
   где относилось к рецепту 80/300/500 и к «5 прогонам ≥ требуемых 4 для mean pass@1»
   (§3 там же). В текущем рецепте это число не пересматривалось ни разу.
3. **Источники называют 3–4** (`HYPOTHESES-WATCH.md:38`), и минимумом нормы — три
   (`laguna-eval-multi-run`). k=3 лежит в этой полосе; k=5 — за её верхом.

**Цена разницы k=5 против k=3** (на исполнимом составе из 6 моделей, сумма факторов 26,8):

| Способ | k=3 | k=5 | Разница |
|---|---|---|---|
| наивно (полный цикл на сид) | 330 сут | 551 сут | **+220 сут** |
| форк (сид множит только RL) | 149 сут | 188 сут | **+39 сут** |

Разница в 5,6 раза — и она целиком внутри решения о семантике сида (§7, пункт «Н-2»).
**Форк решением принят** (`ADR-056` п.2) — значит поднять k до 5 в подтверждающей волне
стоит **39 суток**, а не 220; ветка «без форка» остаётся исторической, а не альтернативой
к выбору. Числа таблицы — ADR-052 при eval ≈2 ч/модель: при объявленной оси они читаются
с поправкой **§5.1а** (там же — почему у В-4 цена другая: состав опорных, а не только eval).

**Семантика сида — «форк»: из открытого вопроса в решение (`ADR-056` п.2, 22.09.2026).**
Прежде семантика сида стояла **открытым вопросом** (`ADR-052` §5; в §7 — пункты «Н-2»/«Н-7»).
Решением владельца от 22.09.2026 она **закрыта**: сид множит **только RL-стадию**, а CPT и
SFT считаются **один раз на модель** (`CPT+SFT+eval + k × RL`). Это **следствие выбора
даты**, а не отдельное предпочтение: без форка волна В-4 (два дополнительных сида) занимает
≈10,2 суток обучения вместо ≈3,7 и выводит область за 1.11 (`ADR-056` п.2, п.1).

**Что форк меняет в чтении результата — и это обязано попасть в формулировку отчёта.**
При форке дополнительные сиды различаются **только RL-фазой**: SFT-чекпойнт у них общий,
поэтому сиды **не независимы** по CPT и SFT. Измеряется, следовательно, **разброс RL-фазы**,
а не разброс всего пайплайна. Формулировка отчёта обязана это нести — рядом с тем, что она
обязана нести про std по трём точкам (абзац ниже). Иначе «устойчиво к смене сида» прочитается
шире измеренного: смена сида **RL** — не то же самое, что смена сида всего цикла (AD-1,
`S4-PROTOCOL.md` §2).

**Носитель решения — раннер, и он сегодня устроен наоборот.** Форк требует, чтобы CPT+SFT
считались один раз на модель, а сид множил RL; исполняемый раннер делает обратное — цикл по
сидам стоит **снаружи** всех стадий (`run_seeded_ladder.sh:11`, §4.1). Это пункт **Н-7** (§7):
без правки раннера решение остаётся на бумаге, а число календаря — недостижимым.

**Чего k=3 не даёт** (назвать до прогона): std по трём точкам имеет ~40 % неопределённости
сам по себе; вывод «эффект устойчив к смене сида» на трёх сидах — это «не опровергнут на
трёх», а не «устойчив». Формулировка отчёта обязана это нести (AD-1, `S4-PROTOCOL.md` §2).

## 5. Волны исполнения

Принцип: **каждая волна даёт результат сама по себе** и не блокирует следующую.
Остановка после любой волны не теряет смысла — теряется только плотность точек.

**Область к 1.11.2026 объявлена решением `ADR-056` (22.09.2026), а не выведена здесь.**
В неё входят **В-0, В-1, В-4**; волны **В-2, В-3 и В-5 отложены за 1.11.2026**. Причина
у всех трёх одна и она не в планировании: полная лесенка требует **≈120 суток машинного
времени при одном стенде** (AD-5: одна нагрузка за раз), а до даты остаётся **36 суток**.
Сжимать рецепт ради даты запрещено (`ADR-056` п.3): сокращённый CPT сделал бы числа
лесенки несравнимыми с ревизией, то есть отнял бы у них силу доказательства. Поэтому
платят **объёмом** — какие оси успевают к дате, а какие уезжают за неё. Цена каждой
отложенной волны названа числом в таблице; перечень отложенного с условием возврата —
`WORK-BACKLOG.md` §4б, чтобы «за датой» не читалось как «забыто».

| Волна | Состав | Что доказывает | Что нужно | Календарь | Область к 1.11.2026 |
|---|---|---|---|---|---|
| **В-0.** Точка решения S4 | Qwen2.5-0.5B, 1 сид, полный цикл — **идёт** | направление оси RL>SFT на одном сиде; закрывает ревизию (C-014) | ничего нового: стадия уже на стенде | **≈3,8 сут** от 21.09 (SFT ≈72 ч + RL 17,5 ч + eval 2 ч) | **входит** — финиш **26.09.2026**; **ось ADR-055 к ней НЕ применяется** (§5.3) |
| **В-1.** Класс ≤1B, три семейства | Qwen3-0.6B + Qwen3.5-0.8B, по 1 сиду (0.5B берётся из В-0) | **воспроизводимость направления между семействами** в одном классе размеров — главная гипотеза `EXPERIMENT_PLAN.md` §1 | веса 0.6B и 0.8B — **на стенде (замер 22.09, §1.2а)**; остаётся преток SFT-кэшей под `qwen3`/`qwen35` и load-test `qwen3_5` (§1.2б) | **11,5 сут** при eval ≈2 ч/модель (**ADR-052**) → **≈12,9 сут (≈13)** посчётом дельты S3bi → **≈14,4 сут** (дельта S3bj: в посчёте S3bi стояло «6 рук ≈ 36 ч» — арифметическая ошибка, 12 × 6 = 72 ч, §5.1). По составляющим — **§5.1а**, единственный носитель посчёта; прежние числа (11,5 и 12,9) не переписываются (ADR-023 п.9). Приписка «+2,4 сут на преток и load-test» **не подтверждена** и слагаемым не берётся: шаг 6 ADR-055 — преток ≈1,5–2 ч стенда, §10 (М-6) — load-test ≈10 мин; в новом числе учтены **эти** цены | **входит** — **11.10.2026** |
| **В-2.** Размер внутри семейства | Qwen2.5-1.5B, Qwen2.5-3B, по 1 сиду | **ось по размеру**: эффект не артефакт 0,5B | ничего нового, кроме весов 3B | **37,0 сут** (ADR-052) → **≈40,8 сут** при объявленной оси (**§5.1а**; нижняя граница) | **отложена за 1.11.2026** — цена ≈40,8 сут |
| **В-3.** Верхний размер | Qwen2.5-7B (LoRA r16), 1 сид | не исчезает ли эффект с ростом модели — единственная точка за пределом «игрушечного» класса | **LoRA r16** (иначе OOM, §3.2); веса 7B | **57,5 сут** (ADR-052) → **≈63,3 сут** при объявленной оси (**§5.1а**; нижняя граница) | **отложена за 1.11.2026** — цена ≈63,3 сут |
| **В-4.** Статистика | +2 сида **форком** на опорных **0,5B и 0,8B** (`ADR-056` п.1; прежде назывались 0,5B и 3B — но 3B уезжает за дату) → k=3 | std и k-из-k по двум опорным точкам — ось AD-1 в полном виде; **разброс RL-фазы**, не всего пайплайна (это следствие форка и обязано попасть в формулировку отчёта, `ADR-056` п.2) | форк-семантика сида — **решено** (`ADR-056` п.2, §4.2) | **10,2 сут** (ADR-052, на 0,5B и 3B) → **≈5,7 сут** при объявленной оси: обучение ≈3,7 + eval 4 рук ≈2,0; счёт — **§5.1а** | **входит** — **17.10.2026** |
| **В-5.** Расширение | остальные 6 моделей заявленного состава | плотность, а не новая ось | по решению, после В-3 | «по 4–74 сут на модель» — границы взяты от **всего состава 12**, а не от остатка; по остатку — **≈244 сут** при объявленной оси (**§5.1а**; 5 моделей из 6 — строки для `Qwen3-1.7B` в §2.3 нет) | **отложена за 1.11.2026** — цена ≈244 сут |

**Суммарно исполнимый состав — прежнее число и новое.** Прежнее: В-0…В-4 =
3,8 + 11,5 + 37,0 + 57,5 + 10,2 ≈ **120 сут** (≈4 месяца) при k=3 и форке; без форка —
330 сут; с В-5 (все 12 моделей, k=3, форк) — 469 сут. Это календарь **ADR-052** при
`eval` ≈2 ч/модель, и он **не переписывается** (ADR-023 п.9) — он остаётся историческим.

**Новое: В-0…В-4 = 3,8 + 14,4 + 40,8 + 63,3 + 5,7 ≈ 128,0 сут — при объявленной оси
и на объявленной области.** Это **нижняя граница** (§5.1а). Две правки в этой сумме
**разного происхождения, и они названы по отдельности**, а не слиты в одну:

- **(а) строка `eval` ушестеряется** объявленной осью (`n = 6`) — это касается В-1,
  В-2, В-3 и В-5 и есть `Решение 1` дельты S3bj; посчёт — **§5.1а**, единственный
  носитель арифметики;
- **(б) состав В-4** переведён на класс ≤1B (0,5B и 0,8B вместо 0,5B и 3B) решением
  `ADR-056` п.1 — это смена предмета статистики, а не пересчёт цены: 3B уезжает за дату
  вместе с В-2. Отсюда 10,2 → 5,7 сут, и это **не** следствие объявленной оси.

**В-2, В-3, В-5 в сумму области не входят** — они за датой (`ADR-056` п.4). Их цена
названа числом отдельно именно поэтому: «за датой» без числа читалось бы как «бесплатно»,
а разница между «отложено» и «забыто» — это и есть наличие счёта.

**Волны идут последовательно, и это не стилистика.** AD-5 запрещает на стенде две
нагрузки разом, поэтому В-1 не стартует раньше снятой точки S4, В-4 — раньше В-1, а
В-5 — раньше различимого результата В-3. Отложенные волны **не запускаются «в параллель,
раз уж считаем»**: это было бы обходом и AD-5, и решения о дате.

### 5.1 Цена eval волны В-1 — названа заранее (`ADR-055` п.6)

Ось объявлена вместе с ценой, а не после прогона: `n = 6` **ушестеряет** eval, и это вписано
в календарь волны, а не обнаруживается по факту.

| Величина | Значение | Откуда |
|---|---|---|
| Цена одной попытки | **≈37,5 с** (калибровка на 0,5B) | ADR-013 п.1 |
| Задач на руку | **192** (`eval_ood_clean.jsonl`, §6.1) | ADR-055 п.2 |
| Попыток на задачу | **6** | ADR-055 п.3 |
| **Цена eval одной руки** | 192 × 6 × 37,5 с = 43 200 с ≈ **12 ч** | ADR-055 п.6 |
| **Цена eval волны В-1** | три семейства × две руки (RL против SFT-базы) = 6 рук × 12 ч = 43 200 × 6 = 259 200 с ≈ **72 ч = 3,0 сут** | ADR-055 п.6; поправка — **ADR-056** |

**Поправка к прежней записи (дельта S3bj).** Прежняя строка читалась «6 рук ≈ **36 ч**» —
и это **внутренне противоречивая запись**: при `n = 6` одна рука стоит 192 × 6 × 37,5 с =
43 200 с ≈ **12 ч**, а шесть рук — **12 × 6 = 72 ч = 3,0 сут**. 36 ч равны **трём** рукам,
а не шести: значит неверна либо подпись, либо итог. Исправлено по решению (`ADR-056`):
было «36 ч» — арифметическая ошибка, исправлено на «72 ч (3,0 сут)». Прежнее число
**не стирается**, а помечается (ADR-023 п.9). Какая именно половина записи была ошибочной —
вопрос не праздный: **числовой носитель оси считает руки иначе** и тоже приходит к 36 ч
(разбор — ниже, «Откуда берётся множитель»), поэтому поправка **введена решением**, а не
выведена из прежней записи — и это различие названо, а не сглажено.

**Как это ложится на уже названный календарь — расхождение названо, а не заглажено.**
В §2.1 строка `eval` — **≈2 ч** на модель (192 задачи, одна рука, ADR-013 п.1). При `n = 6`
та же строка — **≈12 ч на руку**, а на волне В-1 (шесть рук) — **72 ч = 3,0 сут**. Число
календаря В-1 (**11,5 сут**, из ADR-052) здесь **не переписывается** — оно не этой дельтой
выдано; но и не выдаётся за включающее `n = 6`: календарь волны, посчитанный по §2.1, ниже
цены eval по объявленной оси, и разницу читатель видит в этой таблице, а не обнаруживает
по факту.

**Устранено числом в §5.1а (дельты S3bi + S3bj):** календарь В-1 пересчитан по
составляющим — **11,5 сут → ≈12,9 сут (≈13)** посчётом S3bi → **≈14,4 сут** после
исправления цены eval (S3bj). Разница ≈2,9 сут к ADR-052 — **цена различимости**
(eval 5,6 ч → 72 ч) плюс ≈0,1 сут претока и load-test. Прежние числа остаются в тексте
как исторические (ADR-023 п.9), а не стираются.

**Это нижняя граница, и не только для В-1.** Калибровка 37,5 с/попытку снята на **0,5B**;
модели В-1 (0,6B, 0,8B) крупнее её, а размеры В-2…В-4 — кратно крупнее, поэтому **72 ч** —
оценка **снизу**: и для класса ≤1B, и тем более для 7B/9B, где та же попытка дороже.
Верхней границей она не является тем более: термотроттлинг и рост длины генерации
у больших моделей в число не входят (§2.2).

**Откуда берётся множитель и где его граница — расхождение с носителем оси, названное
явно.** 12 ч на руку — это 192 × 6 × 37,5 с, где 37,5 с = 2 ч ÷ 192, то есть цена попытки
**выведена из строки §2.1**, а та по ADR-013 п.1 включает и `eval`, и `eval_sft` на том же
чекпойнте. Тот же вывод стоит в **числовом носителе оси** — `evidence/ladder-axis-arithmetic.json`
(прибор `tools/ladder_axis_arithmetic.py`, дельта S3bg), и там он доведён до конца:

- `eval_cost.calibration`: «≈2 ч на 192 задачи (n=1) **вместе с `eval_sft`** — верхняя граница»;
- `eval_cost.note`: «цена линейна по `m·n`; **число рук волны В-1 — три (по семейству)**».

**По этому носителю волна В-1 стоит 3 руки × 12 ч = 36 ч, а не 6 × 12 = 72 ч**, и прежняя
запись «≈36 ч» не была арифметической опиской сама по себе: 36 ч — **верное число при
трёх руках**, а ошибкой была разве что её расшифровка («три семейства × две руки = 6 рук»
при итоге, равном трём рукам). Поправка на 72 ч введена **решением** (`ADR-056`), а не
выведена здесь, и она опирается на **другой счёт рук**: две руки на семейство (RL-рука и
SFT-база) вместо одной. Оба счёта защитимы, и цена между ними — ровно 2× на этой строке
(≈1,5 сут на волне В-1, то есть ≈1 сутки на дате финиша).

**Что из этого следует, названо, а не сглажено:** носитель оси **не переписывается**
(ADR-023 п.9 — числа, полученные в момент прогона, не правятся задним числом), поэтому
сегодня у числа 72 ч **нет носителя в `evidence/`**, а у 36 ч он есть. Перевыпуск
`evidence/ladder-axis-arithmetic.json` с рукой = «две на семейство» — отдельное решение
(он же носитель порога ≈5,0 п.п., а не только цены) и в эту дельту не входит; вопрос
вынесен архитектору, а не решён молча. Пока он открыт, числа §5.1а читаются **по решению**
(`ADR-056`), а нижняя граница у них двойная — по цене попытки и по числу рук.

### 5.1а Календарь волн по составляющим — единственный носитель посчёта (S3bi + S3bj)

**Правило чтения одно:** где в плане, чек-пойнте или бэклоге стоит календарное число волны,
оно читается **здесь**, а не по прежней записи. Остальные места **ссылаются** на этот
раздел, а не повторяют арифметику: два носителя одного числа расходятся — в кейсе это уже
наблюдалось (ADR-023 п.10).

**Что пересчитано этой дельтой.** Объявленная ось (`n = 6`) **ушестеряет строку `eval`
у каждой волны**, а не только у В-1: у В-2 (1,5B, 3B), В-3 (7B) и В-5 она растёт ровно так
же. Строка `eval` — единственная, которую ось трогает: `CPT`, `SFT` и `RL` идут на прежнем
рецепте (`ADR-056` п.3 — рецепт ради даты не сжимается). Прежние числа не переписываются
(ADR-023 п.9) и стоят рядом с новыми.

**Волна В-1** — по составляющим:

| Составляющая волны В-1 | Чем измерена | При eval ≈2 ч/модель (ADR-052) | При объявленной оси (ADR-055, `n = 6`) |
|---|---|---|---|
| Прогоны **Qwen3-0.6B** (CPT 8,1 + SFT 86,9 + RL 21,0 ч) | §2.3, рецепт §2.1 | 116,0 ч = **4,83 сут** | те же 4,83 сут — ось трогает только `eval` |
| Прогоны **Qwen3.5-0.8B** (CPT 10,7 + SFT 115,9 + RL 28,0 ч) | §2.3 | 154,6 ч = **6,44 сут** | те же 6,44 сут |
| **Преток SFT-кэшей** под `qwen3`/`qwen35` | ADR-055 шаг 6, §7 (Н-1) | в календаре не назывался | **1,5–2 ч** ≈ 0,06–0,08 сут (CPU, но на стенде тем же стеком) |
| **Load-test `qwen3_5`** | ADR-055 шаг 4, §10 (М-6) | в календаре не назывался | **≈10 мин** ≈ 0,01 сут |
| **eval волны** — три семейства × две руки (RL против SFT-базы) = 6 рук × 12 ч | §5.1, ADR-055 п.6 | 5,6 ч = 0,23 сут (2,4 + 3,2 ч по §2.3) | **72 ч = 3,00 сут** |
| **Итого волна В-1** | | **11,5 сут** (ADR-052) | **≈14,4 сут** |

**Строка решения В-1:** 11,5 сут при eval ≈2 ч/модель (ADR-052) → **≈12,9 сут (≈13)**
посчётом S3bi → **≈14,4 сут** после исправления цены eval (S3bj). **Разница к ADR-052
≈2,9 сут — цена различимости** (eval 5,6 ч → 72 ч, то есть +2,77 сут; преток и load-test
добавляют ≈0,07–0,09 сут).

**Волны В-2, В-3, В-5 — тот же посчёт по составляющим.** Числа — **нижние границы**:
37,5 с/попытку калиброваны на 0,5B, а здесь модели крупнее, поэтому та же попытка дороже
(§5.1, §2.2).

| Волна | Модели (строка `eval` §2.3 в скобках) | `eval` при ADR-052 | `eval` при `n = 6` | Не-eval часть (CPT+SFT+RL) | Было (ADR-052) | **Стало (при объявленной оси)** |
|---|---|---|---|---|---|---|
| **В-2** | Qwen2.5-1.5B (6,0 ч), Qwen2.5-3B (12,0 ч) | 18,0 ч = 0,75 сут | 108,0 ч = **4,50 сут** | 36,25 сут | 37,0 сут | **≈40,8 сут** |
| **В-3** | Qwen2.5-7B (28,0 ч) | 28,0 ч = 1,17 сут | 168,0 ч = **7,00 сут** | 56,33 сут | 57,5 сут | **≈63,3 сут** |
| **В-5** | Qwen3.5-2B, Qwen3-4B, Qwen3.5-4B, Qwen3-8B, Qwen3.5-9B | 108,0 ч = 4,50 сут | 648,0 ч = **27,00 сут** | 217,39 сут | «по 4–74 сут на модель» | **≈244,4 сут** |

Строка `eval` в колонке ADR-052 — сумма по §2.3 (на модель **одна** рука, `n = 1`);
не-eval часть — та же сумма за вычетом `eval`. `n = 6` ушестеряет ровно строку `eval`,
остальные стадии не трогает. Так, у В-3 число сошлось с ожиданием: **28 ч → 168 ч**,
57,5 → **≈63,3 сут** (а не ≈63,0 — округление до десятых, не подгонка).

**Волна В-4 — пересчитана не осью, а составом.** `ADR-056` п.1 переводит статистику на
класс ≤1B (**0,5B и 0,8B**): 3B уезжает за дату вместе с В-2. Отсюда **≈5,7 сут**:
обучение 91 ч (2 доп. сида × (RL 17,5 ч на 0,5B + RL 28,0 ч на 0,8B)) ≈ 3,8 сут + eval
дополнительных рук 4 × 12 ч = 48 ч = 2,0 сут. **Это смена предмета статистики, а не
пересчёт цены** — ось здесь уже учтена, потому что ADR-056 назвал цену руки по `n = 6`.
Прежнее 10,2 сут (на 0,5B и 3B, ADR-052) не стирается. Мелкое расхождение **названо**:
91 ч = 3,79 сут, а ADR-056 пишет «обучение ≈3,7 сут» — разница 0,1 сут от округления,
сумме не мешает.

**Границы посчёта — названы, чтобы число не читалось шире факта:**

- **Все числа — нижние границы** (§5.1): 37,5 с/попытку калиброваны на 0,5B, а модели
  В-1…В-5 крупнее; термотроттлинг и рост длины генерации в число не входят (§2.2).
- **Руки считаются по-разному в разных строках — и это названо, а не спрятано.** У В-1
  строка `eval` считает **две** руки на семейство (RL против SFT-базы, 6 рук), потому что
  парное сравнение и есть её предмет. У В-2/В-3/В-5 та же строка — **шестикратная** цена
  руки по §2.3, где на модель учтена **одна** рука. Если считать их как В-1 (две руки на
  модель), строка `eval` **удваивается** (В-2: 9,0 сут, В-3: 14,0 сут, В-5: 54,0 сут),
  и числа становятся ≈45,2 / ≈70,3 / ≈271,4 сут. Сказано прямо: числа таблицы — **нижние
  границы в двух смыслах** — по цене попытки и по числу рук.
- **`Qwen3-1.7B` в В-5 не посчитан**: строки для него в §2.3 нет (таблица по размерам
  покрывает 11 моделей из 12). Поэтому ≈244 сут — цена **5 моделей из 6**: это пропуск
  в носителе посчёта, названный, а не замолчанный.
- **преток и load-test прежним календарём не назывались вовсе**, и приписанные строке В-1
  «+2,4 сут» **не подтверждены**: шаг 6 ADR-055 даёт **1,5–2 ч**, §10 (М-6) — **≈10 мин**.
  Прежняя цифра не стирается (git-история) и названа исторической; в числах этой таблицы
  она **слагаемым не берётся нигде** — вместо неё стоят цены, снятые с носителей.
  Источник приписки **не ищется и ошибкой не объявляется** (`ADR-056`): происхождение
  старой записи не меняет ни одного решения, а объявлять ошибкой непроверенное — тот же
  класс, что и молчание.
- **сумма §5 пересчитана этой дельтой целиком** (В-0…В-4 = ≈128,0 сут): прежний открытый
  вопрос «пересчитывать ли В-2…В-5» закрыт, а не оставлен половиной.

**Чек-пойнты решений** (остановка — законный исход, а не провал):

| После волны | Решение | Если «нет» |
|---|---|---|
| В-0 | точки решения S4 (`S4-PROTOCOL.md` §3, четыре критерия) | стоп лесенки: ось RL>SFT не подтверждена даже на одном семействе — сравнивать нечего |
| В-1 | **порог оси §6.1 пройден самостоятельно в ≥2 из 3 семейств** (под нулём 0,725 %; формулировка «по знаку» из `EXPERIMENT_PLAN.md` §6.1 **отвергнута** — 50 % под нулём) | стоп с формулировкой «эффект семейство-специфичен»: это **валидный отрицательный результат**, расширять нечего |
| В-2 | эффект не монотонно убывает с размером | остановка В-3: **57 сут** (при объявленной оси — **≈63,3 сут**, §5.1а) на верхний размер при убывающем эффекте не оправданы |
| В-3 | эффект различим на 7B | В-5 закрывается как ненужная: ось отвечена |
| В-4 | std существенно меньше эффекта; иначе k=5 в В-4бис (+39 сут форком — число ADR-052) | формулировка отчёта — «неразличимо на k сидах» |
| **В-1, до старта В-4** (чек-пойнт `ADR-056`) | **условие пересмотра области:** если к финишу В-1 (**≈10.10**) буфер до 1.11 сохранится **≥10 сут**, рассматривается добавление **одной точки по размеру (1,5B)** на том же рецепте. Это **уточнение области, а не переигрывание оси** (та же формулировка, что в §6.1 для порога); решение — за владельцем | буфер <10 сут (или съеден сбоем стенда) — область остаётся В-0/В-1/В-4, и 1,5B уезжает за дату вместе с В-2 |

**Числа календаря в этой таблице** (57; +39; +10,2) выданы ADR-052 при eval ≈2 ч/модель.
Правило чтения одно: где в решении стоит календарное число, оно читается **по §5.1а** —
с поправкой на `n = 6` — а не берётся в прежнем значении. Так, решение по В-3 принимается
не по 57 сут, а по **≈63,3 сут** (из них 39 сут форком — RL-строка, осью не затронутая).

### 5.2 Календарь области к 1.11.2026: волна → дата → что зависит

Длительность без даты читалась бы как «план без срока»; `ADR-056` п.5 называет и то, и
другое. Даты отсчитываются от точки S4 (**26.09.2026**), потому что В-1 не стартует раньше
неё (`ADR-056` п.5; `S4-PROTOCOL.md` §3).

| Волна | Длительность (§5.1а) | Дата | Что зависит от неё |
|---|---|---|---|
| **В-0** | ≈3,8 сут (идёт) | **26.09.2026** | точка решения S4; без неё В-1 не стартует (последовательность волн — AD-5). От этой дельты не зависит ничем |
| **В-1** | ≈14,4 сут | **11.10.2026** | чек-пойнт `ADR-056`: к этой дате решается, сохранился ли буфер ≥10 сут на точку 1,5B. Открывает В-4: её опорные 0,5B и 0,8B обучены внутри В-1 |
| **В-4** | ≈5,7 сут | **17.10.2026** | **финиш области**; ось AD-1 (std и k-из-k) в полном виде. Ни одна отложенная волна для неё не нужна |
| **Буфер** | ≈14 сут (**17.10 → 01.11 = 15 календарных суток**) | до **01.11.2026** | **назначение названо, а не подразумевается: сбои стенда.** Класс уже стоил суток (одна остановка по предохранителю), сюда же память и тепло площадки. Буфер — не «остаток срока», а статья: любая чужая нагрузка на том же стенде его съедает, и тогда дата становится целью, а не следствием |
| В-2, В-3, В-5 | ≈40,8 / ≈63,3 / ≈244,4 сут | **за 01.11.2026** | отложены решением (`ADR-056` п.4); условие возврата — `WORK-BACKLOG.md` §4б. **Дописывать волну «раз уж считаем» нельзя** — это новое решение владельца, а не уточнение (`ADR-056` п.7) |

**Сведение с `ADR-056` — и расхождение, которое называется, а не подгоняется.** Чистое
сложение длительностей даёт 26.09 + 14,4 ≈ **10.10** и + 5,7 ≈ **16.10**; `ADR-056` называет
**11.10** и **17.10**. Разница ≤1 суток на волну — округление до конца суток при переводе
длительностей в даты. **Даты берутся по ADR-056** (решение о них объявлено там), а
расхождение остаётся видимым: на вывод «финиш ≈17.10, буфер ≈14 сут» оно не влияет, но
подгонять одну сторону под другую значило бы потерять след посчёта.

### 5.3 Ось `ADR-055` к волне В-0 **не применяется** — прогон начат до решения

Прямая норма `ADR-055` п.7: ось читается **только к прогонам, начатым после решения** —
исторические результаты под неё не подтягиваются. **В-0 начат раньше**: его SFT-стадия
идёт с 21.09.2026, решение ADR-055 принято 22.09.2026. Отсюда:

- В-0 оценивается **своим протоколом** — `S4-PROTOCOL.md` (фаза 1: **один сид**,
  `single_seed: true`, `n_attempts = 1`, `std` и `k_of_k` — `null` с пометкой). Числа В-0
  читаются **по `S4-PROTOCOL.md`, а не по оси лесенки**;
- поэтому у В-0 и В-1 **разные `n`** (1 и 6), и это **не рассогласование, а порядок
  применения** оси (п.7). Вопрос «почему у В-0 и В-1 разные `n`» снят здесь **до** того,
  как он возникнет при чтении отчёта;
- **0,5B-строка волны В-1 — не то же, что В-0.** Прогоны В-1 не повторяют: 0,5B-чекпойнт
  берётся из В-0. Но **сравнение RL против SFT-базы внутри В-1 идёт при `n = 6`**, то есть
  руки по этой оси переоцениваются — именно поэтому в строке `eval` волны В-1 стоят **три**
  семейства, а не два (§5.1а). Это чтение счёта, названное явно; если 0,5B-руки в В-1 **не**
  переоцениваются (два семейства вместо трёх), цена eval волны падает на 24 ч (≈1,0 сут),
  а В-1 — до ≈13,4 сут. Вопрос вынесен архитектору, а не решён задним числом.

## 6. Ось лесенки: **объявлена** до первого прогона (`ADR-055`, 22.09.2026)

**Конфликт, который обязан быть разрешён в ADR-052 п.2, а не обойдён.** `AD-1`
(`ARCHITECTURE-SPINE.md:19`) прямо запрещает то, ради чего лесенка существует:

> «Заявления о превосходстве между семействами/размерами и внешние сравнения запрещены:
> протокол их не покрывает.»

Это запрет **узкого протокола ревизии**, и он верен: 192 задачи, один сид, SE ≈3,6 %
не покрывают сравнение семейств. Лесенка — другой предмет, и она не отменяет AD-1,
а **вводит вторую ось** — объявленную **до** первого прогона по тому же принципу
(«ось успеха одна и объявлена до прогона»).

**Поправка, названная явно, а не задним числом.** Число «SE ≈3,6 % при 192 задачах»,
стоявшее здесь прежде (и в `AD-1`), — это SE **одной руки**; для сравнения двух рук
релевантна SE **разности**, и при тех же 192 задачах она **5,10 %**. То есть **прежний
порог был занижен в √2**. Прежняя формулировка не переписывается: она остаётся в
git-истории этого файла и в `AD-1`, а носитель поправки — `evidence/ladder-axis-arithmetic.json`
(прибор `tools/ladder_axis_arithmetic.py`, дельта S3bg, Н-6) и `ADR-055`.

**Здесь прежде стоял список «чего в этой оси ещё нет»** — четыре поля (метрика и `n`,
порог различимости, что считается воспроизведением, правка спайна). Список закрыт
решением: ось объявлена **`ADR-055`** (22.09.2026), и ниже — её объявленные значения,
а не перечень недостающего.

### 6.1 Объявленная ось (`ADR-055`, 22.09.2026) — значения

| Поле | Объявленное значение | Пункт ADR-055 |
|---|---|---|
| **Метрика** | `judge_mean` по ответам с вердиктом verifier'а (`multi_slug_match` 0/1) + `slug_accuracy` | п.1 |
| **Сравнение** | **парное внутри семейства**: RL-рука против SFT-базы того же чекпойнта того же семейства. Не абсолютные числа между семействами | п.1 |
| **Набор** | `datasets/eval_ood_clean.jsonl` — **192** задачи `ood_pair`, sha256 `f94fb855…`. Новых наборов не создаётся | п.2; AD-7 |
| **Вердикт задачи** | verifier `multi_slug_match` | п.2 |
| **`n` попыток** | **6** на задачу. Pass@k за пределы измеренного `n` **не экстраполируется** | п.3; ADR-015 |
| **Порог различимости** | **≈5,0 п.п.** (**4,99** при `m=192, n=6`): 95 % **+ поправка Бонферрони на три семейства**, мощность 80 %. Различие **ниже порога не заявляется** — оно называется «неразличимо на этом наборе», а не «эффект есть» | п.4 |
| **Критерий воспроизведения** | порог пройден **самостоятельно** в **≥2 семействах из 3** — под нулём **0,725 %**. Вариант «по знаку» **отвергнут**: под нулём 50 % (критерий не различает ничего); «3 из 3» как обязательное требование — перебор для первой волны (0,013 %) | п.5 |
| **Цена eval** | ≈**12 ч** на руку при `n=6` (192 × 6 × 37,5 с); волна В-1 — **≈72 ч = 3,0 сут** по решению `ADR-056`. Записанное в `ADR-055` п.6 «≈36 ч» **не стирается** и названо историческим: оно верно при **трёх** руках (по семейству), а поправка введена счётом **двух** рук на семейство — разбор в §5.1. Для крупных размеров — **нижняя граница** | п.6; ADR-013 п.1; поправка — `ADR-056` |
| **Порядок применения** | ось читается **только к прогонам, начатым после решения**. Исторические результаты лесенки к ней не подтягиваются. Смена набора / `n` / порога — **новое решение**, а не «уточнение по ходу». **Следствие для этого плана: к волне В-0 ось не применяется** — прогон начат 21.09, до решения; числа В-0 читаются по `S4-PROTOCOL.md` (§5.3) | п.7 |
| **Что ось не покрывает** | систематику: смену площадки (4080 ↔ GB10), смену режима декодирования, утечку набора в обучение. SE её не видит — это контроль отдельными механизмами (пиннинг площадки и прибора) | п.8 |

**Необратимость по сопоставимости.** До старта В-1 параметры оси — числа в плане, и
смена дешева; **после** старта В-1 смена `n`, набора или порога делает прогон
несравнимым с уже сделанным (тот же класс, что смена оси).

**Условие пересмотра** (названо заранее, ADR-055 `Reversibility`): если эффект окажется
на границе (**2–5 п.п.** и при этом согласован по знаку в ≥2 семействах) — выносится
решение о расширении набора (`m = 400+`, аудит чистоты по ADR-025). Это **уточнение
разрешения**, а не переигрывание оси.

**Носителей у оси два, и оба открываются:** `docs/adr/ADR-055-os-lesenki-metrika-nabor-n-porog-razlichimosti.md`
(решение с таблицей SE и с ценой) и `evidence/ladder-axis-arithmetic.json` (числа,
воспроизводимые одной командой). Числа к решению (S3bg) — `docs/specs/LADDER-AXIS-PROPOSAL.md`;
он остаётся носителем **предложения**, а не объявленной оси: объявляет ось ADR.

## 7. Что можно начать немедленно — без нового железа и без стенда

**Состояния на 22.09.2026 (дельта S3bh).** Закрыты: **Н-3** (веса на стенде — 11 из 12),
**Н-5** (карточки AD-2 на три семейства) и **Н-6** (ось объявлена — `ADR-055`).
Открыты и перечислены ниже: **Н-1** (преток — первый шаг подготовки В-1, требует стенда),
**Н-7** (правка раннера под форк — исполняемый носитель решения), **Н-8** (память на 1,5B/3B —
требует стенда). Плюс отдельная фаза подготовки В-1 — **load-test `qwen3_5`** (§1.2б, §10, М-6).
**Н-2** из этого перечня **выбыл**: решение о семантике сида принято 22.09.2026 (`ADR-056` п.2,
§4.2) — открытым остаётся только его носитель (Н-7). Живая сводка предусловий —
`WORK-BACKLOG.md` §4а.

| # | Работа | Цена | Что даёт |
|---|---|---|---|
| **Н-1** | **Претокенизировать `sft_train_v13_fixed` под токенизаторы `qwen3` и `qwen35`** — **первый шаг подготовки В-1 и место его выполнения названо**: преток идёт **на стенде тем же стеком**, что и стадии | CPU-only (раннер и так зовёт преток с `CUDA_VISIBLE_DEVICES=""`), часы, NFS | снимает из В-1 обязательный шаг; сейчас есть только `…_qwen25.npz` (`full_sft_params.json`) |
| **Н-1 (статус 22.09.2026, уточнён S3bh)** | **не выполнено — почему и во сколько это встанет; место и требование к результату названы** | ≈1,5–2 ч стендового времени на два тега (или ≈1 ч вне стенда, если починить локальный env) | замером Н-5 подтверждено: кэш **текущего рецепта** (v13_fixed) есть только под `qwen25`; под `qwen3`/`qwen35` **не собран** (`data/ad2-cards/`). Кэши **исторического** рецепта (`sft_train_v12_8192_*`) при этом есть под все три тега — но это факт о дереве, а не закрытый вход волны: вход объявлен как v13_fixed (ADR-050), и подмена рецепта ради готового кэша была бы сменой предмета сравнения. Вне стенда шаг упирается в два факта: токенизатор `qwen35` лежит **только в локальном кэше стенда** (на рабочую станцию не виден — не NFS), а локальный интерпретатор `~/miniconda3/bin/python3` имеет сломанную пару `transformers` ↔ `huggingface-hub 1.27.0`. Для `qwen3` препятствие снимается без стенда: локальный `Qwen/Qwen3-4B` несёт **тот же** `tokenizer.json` (sha256 `aeb13307…`, сверено), что и `Qwen3-0.6B`. Выход: 2 890 465 888 Б на тег (замер по `qwen25`) ≈ 5,8 ГБ на NFS при 825 ГБ свободных. **Место: стенд, тем же стеком** — чинить локальный `transformers` **не надо**: второй стек стал бы источником расхождения (AD-3), и кэш, снятый другим токенизатором, был бы не тем же входом, что вход стадии. **Требование к результату: кэш объявляется карточкой AD-2** (`data/ad2-cards/ad2-{qwen3,qwen35}.json`, поле `tokens_cache.present` со **sha256, снятым прибором** `tools/build_ladder_cards.py --hash-caches`), а **не именем файла**: у кэша и у токенизатора разные носители факта (ADR-051), и «файл с ожидаемым именем» весами кэша не считается |
| **Н-2** | ✅ ~~**Решение о семантике сида: сид множит только RL**~~ **решено 22.09.2026 — `ADR-056` п.2 (семантика «форк»)**; остаётся носитель — **Н-7**. Обоснование и ограничение (сиды не независимы по CPT/SFT, измеряется разброс RL-фазы) — §4.2 | решение принято; **$0** | 330 → 149 сут на исполнимом наборе (2,2×); дополнительные сиды 4,11 → 0,73 сут на 0,5B. **Без форка волна В-4 выходит за 1.11** — это и есть цена даты (`ADR-056` п.2) |
| **Н-3** | ~~**Проверить наличие весов лесенки на стенде**~~ **выполнено 22.09.2026 (S3bg)** | read-only, GPU не занят | сняло главную неизвестность §1.2: на стенде **11 из 12** (§1.2а), отчёт `evidence/ladder-weights-inventory.json` |
| **Н-4** | ~~**Докачать веса шести исполнимых моделей**~~ **сокращено замером 22.09.2026 (§1.2а)**: качать нужно **одну** — `Qwen3-8B` (≈16,4 ГБ); **решением (ADR-055 шаг 5) — не докачивать** | трафик + место на NFS | ~~внешняя зависимость критического пути~~ — теперь зависимость только расширения В-5. **`Qwen3-8B` не докачивается и предусловием волн В-0…В-4 не является**: отсутствие названо (нет снапшота — пустой блоб, ≈16,4 ГБ при 825 ГБ свободных на `gb10-shared`), но критический путь оно не держит. Расширение (**В-5**) — **только после В-3** с различимым (по оси §6.1) эффектом, §9 |
| **Н-5** | ~~**Собрать карточки AD-2 на три семейства**~~ **выполнено 22.09.2026 (S3bg)**: `data/ad2-cards/ad2-{qwen25,qwen3,qwen35}.json`, прибор `tools/build_ladder_cards.py` | CPU | без них прогоны лесенки несопоставимы и недоказуемы — ровно то, чем ADR-001 отклоняла вариант «воспроизвести лесенку как есть». Карточка `qwen25` дополнена (её не было), `qwen3`/`qwen35` составлены впервые; отсутствие кэшей записано фактом, а не «предполагается» |
| **Н-6** | ✅ ~~**Объявить ось и порог различимости лесенки**~~ **выполнено 22.09.2026 — решение `ADR-055`, ось объявлена (§6.1)**: метрика `judge_mean`+`slug_accuracy`, `m=192`, `n=6`, порог **≈5,0 п.п.**, критерий «≥2 из 3 по порогу» | часы, арифметика (сделано: `tools/ladder_axis_arithmetic.py`) | без этого первый же результат В-1 был бы заявлением, которого протокол не покрывает. Носители: `docs/adr/ADR-055-os-lesenki-metrika-nabor-n-porog-razlichimosti.md` + `evidence/ladder-axis-arithmetic.json`; предложение с числами — `docs/specs/LADDER-AXIS-PROPOSAL.md` |
| **Н-7** | **Отделить «полный цикл» от «сида» в `run_seeded_ladder.sh`** | правка скрипта | то же, что Н-2, но в исполняемом носителе: сейчас цикл по сидам снаружи всех стадий |
| **Н-8** | **Измерить память на 1,5B и 3B** (50–100 шагов, `mem_sampler`) | **стенд**, ~1 ч на размер | закрывает §3.2: сейчас «3B влезает» — исторический факт без запаса |

**Чего делать НЕ надо (и почему):**

- **Распараллеливать сиды по устройствам** (часть на 4080, часть на GB10) — числа прибора
  **не переносятся между 4080 и GB10** (57 из 104 совпавших ответов), и тогда сравнение
  семейств окажется конфаунднуто сменой площадки, а не семейством. Плюс AD-5: две
  тренировочные нагрузки на GB10 запрещены.
- **Переиспользовать остановленный прогон `sft-20260919-0810`** как сид лесенки — он на
  другом наборе (`v12`, без контроля длины), и ADR-050 объявил его референсом, а не входом.
- **Считать «12 × 5» выполнимой задачей с каким-то темпом** — §2.4: 4,74 года наивно.

## 8. Требования к ресурсам по волнам

| Ресурс | В-0 | В-1 | В-2 | В-3 | В-4 |
|---|---|---|---|---|---|
| **Область к 1.11** | **входит** (26.09) | **входит** (11.10) | **отложена за дату** | **отложена за дату** | **входит** (17.10) |
| Стенд GB10 | **занят** | занят 11,5 сут → ≈12,9 сут (S3bi) → **≈14,4 сут** при объявленной оси (§5.1а) | занят 37 сут → **≈40,8 сут** (§5.1а) — **за датой** | занят 57,5 сут → **≈63,3 сут** (§5.1а) — **за датой** | занят 10,2 сут (на 0,5B и 3B) → **≈5,7 сут** (`ADR-056` п.1: опорные 0,5B и 0,8B) |
| Веса | есть | 0.6B, 0.8B — **есть на стенде (замер 22.09)** | 1.5B есть, 3B — **есть на стенде (замер 22.09)** | 7B — **есть на стенде**, нужен **LoRA r16** | — |
| Память стадии | 50 ГБ порог соблюдён | то же (≤0,8B) | 1,5B проходит; 3B — запас не измерен | **LoRA обязателен**, иначе OOM | — |
| Новые механизмы | — | load-test `qwen3_5` (`tools/qwen35_gate.sh`, §1.2б-1) | — | LoRA-путь в пайплайне | форк-семантика сида — **решена** (`ADR-056` п.2); остаётся правка раннера (**Н-7**) |
| Локальная 4080 | не нужна | преток (CPU) | преток | — | — |
| Окно монополии стенда (AD-9, ADR-012) | продлевать | продлить + вернуть ≤ суток | то же | то же | то же |

**AD-9/ADR-012 в силе:** на время каждой волны стенд принадлежит ей; автономный контур
лесенки v12 остаётся приостановленным (флаг `REVISION-WINDOW.lock` + `chmod -x` сторожа +
снятый `@reboot`, четыре шага — без третьего первые два бесполезны).

## 9. Риски

| Риск | Вероятность | Цена | Что делаем |
|---|---|---|---|
| ~~**Веса 10 из 12 моделей недоступны на стенде**~~ **снят замером 22.09.2026 (S3bg)**: недоступна **1** модель (`Qwen3-8B`, ≈16,4 ГБ) | **низкая** (была высокая — оценивалась по общему стору); для В-0…В-4 не блокирует вовсе | блокирует только расширение В-5 | докачка одной модели (Н-4) **перед В-5**, не перед В-1; §1.2а |
| **`qwen3_5` не поддержан образом** (`transformers`/`vLLM`) | **средняя и по-прежнему непроверенная** (замер 22.09: у гейта `qwen35_gate.sh` **вердикта нет** — он ждал чужую лесенку; HF-путь на 2B исторически проходил, vLLM-путь не проверялся — §1.2б) | семейство выпадает из В-1, ось «между семействами» схлопывается до 2 семейств | ~~load-test отвязать от завершения чужой лесенки~~ **отвязан (S3bh, ADR-055 шаг 4): `tools/qwen35_gate.sh`**, фаза подготовки В-1 с названным условием (≈10 мин окна, §1.2б-1, М-6). Осталось **выполнить её** перед В-1; при фейле — решение владельца: 2 семейства или замена |
| **3B не влезает по памяти с платформенным сервером** | средняя (никто не мерил) | В-2 теряет верхнюю точку | Н-8: замер до объявления волны |
| **Календарь не выдержан** (оценка — нижняя граница, §2.2) | высокая на длинных волнах | +20–30 % к В-2/В-3 | чек-пойнты решений после каждой волны; волны не объявляются «под ключ» |
| **Лесенка конкурирует с ревизией за стенд** | **уже реализовалась** (решение владельца сняло условие возврата до точки S4) | ревизия и лесенка встают в очередь друг за другом | явная очередь волн; В-1 не стартует раньше, чем снят результат В-0 |
| ~~**Ось лесенки объявлена после первого прогона**~~ **снят 22.09.2026 (S3bh)**: ось объявлена **`ADR-055`** до первого прогона волны В-1 (§6.1) — и метрика, и набор, и `n=6`, и порог ≈5,0 п.п., и критерий | **снята** | ~~результат В-1 недоказуем — подгонка метрики после прогона, ровно то, что запрещает AD-1~~ | Н-6 закрыт: числа посчитаны 22.09.2026 (`evidence/ladder-axis-arithmetic.json`), решение — ADR-055. Остаточный риск — **не систематика, а разрешение**: эффект 2–5 п.п. на этом наборе заявлен не будет (условие пересмотра — §6.1) |
| **Данные `internal_only` уезжают на арендованные GPU** | низкая (только если выбран вариант C ADR-052) | нарушение AD-8 | аренда — только через отдельное решение о границе |

## 10. Что нужно измерить (и сколько это стоит)

| Замер | Где | Цена | Что закрывает |
|---|---|---|---|
| **М-1.** Наличие весов лесенки | стенд, read-only | минуты | ~~единственная незакрытая неизвестность состава~~ — **выполнено 22.09.2026 (S3bg): 11 из 12, §1.2а** |
| **М-2.** Память стадии на 1,5B и 3B (50–100 шагов с `mem_sampler`) | стенд | ~1 ч на размер | §3.2: «влезает» против «влезает с запасом» |
| **М-3.** Цена шага SFT при батче 1 против батча 2 на 0,5B | стенд | ~1 ч | проверяет допущение §2.2 (что время ∝ токенам, а не шагам) |
| **М-4.** `tok/s` генерации RL на 1,5B | стенд | ~2 ч (мелкая проба) | верхняя граница RL-стадии: у 0,5B генерация уже упирается в потолок хода (97,6 %) |
| **М-5.** SE межсемейственного сравнения на 192 задачах | арифметика, CPU | $0 | ~~порог различимости оси лесенки~~ — **выполнено 22.09.2026 (S3bg), расширено до сетки `m × n`: `docs/specs/LADDER-AXIS-PROPOSAL.md`, `evidence/ladder-axis-arithmetic.json`** |
| **М-6.** Load-test `qwen3_5` (архитектура `qwen3_5` стеком `transformers` + `vLLM`, модель `Qwen/Qwen3.5-0.8B-Base`) | **стенд** (`tools/qwen35_gate.sh run --host gb10-fast`) | **≈10 мин**, окно без живой тренировочной нагрузки (AD-5/AD-9) | §1.2б: **не проверено и по сей день** — прежний гейт ждал чужую лесенку и не выполнился ни разу. Вердикт (три состояния) — `evidence/qwen35-loadtest.json`; **фаза подготовки В-1**, отвязана от чужого события дельтой S3bh. В S3bh **не запускалась** (стенд занят SFT) |
| **М-7.** Перевыпуск носителя оси под счёт рук, принятый решением (`eval` = **две** руки на семейство) | арифметика, CPU | **$0** | **названо, а не умолчано:** `evidence/ladder-axis-arithmetic.json` (дельта S3bg) несёт `eval_cost.note` «число рук волны В-1 — **три** (по семейству)» и калибровку «≈2 ч … **вместе с `eval_sft`**», то есть **36 ч**, а не 72 ч. Носитель оси этой дельтой **не переписывается** (ADR-023 п.9), поэтому у числа 72 ч **носителя в `evidence/` нет**, а у 36 ч он есть (§5.1). Перевыпуск — **отдельное решение**, а не побочная правка: тот же файл несёт и порог ≈5,0 п.п., и трогать его в дельте о цене eval значило бы менять два предмета одной правкой |

**Порядок подготовки В-1** (обе позиции требуют стенда, но это **разные** окна и разный
предмет: преток — CPU, load-test — GPU):

1. **Преток SFT-кэшей** под `qwen3`/`qwen35` — **на стенде тем же стеком** (Н-1, §7);
   результат объявляется карточкой AD-2, а не именем файла.
2. **Load-test `qwen3_5`** (М-6, §1.2б-1) — ≈10 мин окна.

**Чего этот план не доказывает:**

- что лесенка **даст** воспроизведение эффекта — это её гипотеза, а не факт;
- что цена по размерам линейна — это **названное допущение** (§2.2), проверяется М-3/М-4;
- что заявленный состав (12) достижим на этом стенде — §2.4 и §3.2 говорят обратное
  для верхних размеров без LoRA;
- что «нужна статистическая мощность» — исходное условие возврата спайна — **выполнено**:
  оно снято решением владельца, а не исполнением (§0).
